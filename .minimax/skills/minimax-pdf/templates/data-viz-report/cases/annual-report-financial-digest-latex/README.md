# Annual report financial digest — compact LaTeX case

This case stores editable artifacts for the workflow documented in:

`../../../../docs/annual-report-financial-digest-latex-case.md`

Files:

- `source.tex` — compact data-table-first LaTeX report template.
- `build_charts.py` — chart/data scaffold from the PetroChina annual-report run; update values and paths before reuse.

Do not commit generated PDFs here. Compile into a task-specific working directory.
